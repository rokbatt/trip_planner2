-- ============================================================
-- 정류지 "체류시간 직접 지정" 컬럼
-- Supabase 대시보드 → SQL Editor 에 붙여넣고 실행하세요.
-- 여러 번 실행해도 안전(idempotent)합니다.
--
-- 왜 필요한가
--   TIMELINE의 "예상 체류"는 지금까지 방문 유형(관광/맛집/액티비티 등) 6종에 대한 고정값
--   테이블(dwellMinutes, 원칙 3-1 — 실제 데이터가 아니라 참고용 추정치)만 썼음. 장소 규모나
--   실제 계획과 무관하게 같은 카테고리면 전부 같은 숫자였는데, 사용자가 "여긴 2시간 있을
--   거야"처럼 직접 정할 방법이 없었음. 이 컬럼이 그 값을 저장한다 — null이면 지금처럼
--   카테고리 기본값을 그대로 쓴다(dayModel.ts의 effectiveDwellMinutes 참고).
-- ============================================================

alter table if exists route_stops add column if not exists custom_dwell_min integer;

-- 완료.
