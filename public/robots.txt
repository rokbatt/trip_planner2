# 지금은 색인할 공개 페이지가 랜딩(/) 하나뿐이다.
# 트립 내부는 해시 라우터(#trip/...) 위에 있어 크롤러가 접근하지 못하고,
# 애초에 초대받은 멤버만 보는 공간이라 색인 대상이 아니다.
User-agent: *
Disallow: /api/

# 목적지 소개 같은 공개 콘텐츠 페이지를 프리렌더해서 붙이게 되면
# 그때 Sitemap 항목을 여기에 추가한다.
